= form_for [target, Comment.new], remote: true, data: { parent_name: target.class.name.downcase.pluralize } do |f|
  %fieldset
    .comment-errors
    = f.label :body
    = f.text_area :body
    = f.submit "Сохранить комментарий"